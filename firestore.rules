rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to everyone for now (Development Mode)
    // IMPORTANT: secure this before going to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
